apiVersion: apps/v1
kind: Deployment
metadata:
  name: testmain-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: testmain
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1"
        # Configuraciones adicionales para producción
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
