apiVersion: apps/v1
kind: Deployment
metadata:
  name: testmain-app
spec:
  template:
    spec:
      volumes:
      - name: temp-volume
        emptyDir: {}
      containers:
      - name: testmain
        volumeMounts:
        - name: temp-volume
          mountPath: /tmp
