apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test

resources:
  - namespace.yaml
  - ../../base
  - mysql-pv.yaml
  - mysql-pvc.yaml

images:
  - name: ghcr.io/brunoredolfi/testmain
    newTag: test-7209601b045c15137c7c3c6d82614fe695bb377d

patches:
  - target:
      kind: Deployment
      name: mysql
    patch: |-
      - op: replace
        path: /spec/template/spec/volumes/0/persistentVolumeClaim/claimName
        value: mysql-pvc-test
