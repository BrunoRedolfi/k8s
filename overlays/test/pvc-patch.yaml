apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-test  # New unique name
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
