apiVersion: v1
kind: Service
metadata:
  name: testmain-service
spec:
  type: ClusterIP  # Acceso interno (para integrar con Ingress si es necesario)
  ports:
  - name: http      # ← CAMPO REQUERIDO que faltaba
    port: 80        # Puerto del service
    targetPort: 8080 # Puerto de tu app Spring Boot
    protocol: TCP
